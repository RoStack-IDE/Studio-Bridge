--!strict
-- RbxDom: Roblox DOM utilities for property encoding/decoding
-- Ported from Rojo's rbx_dom_lua for complete Roblox type support

local EncodedValue = require(script.EncodedValue)

return {
	EncodedValue = EncodedValue,
}
